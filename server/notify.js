/**
 * 每位工程师都有保持代码优雅的义务
 * Each engineer has a duty to keep the code elegant
 *
 * @author wangxiao
 */

// 用户通知相关方法

'use strict';

const tool = require('./tool');

let pub = {};

// 发送短信
pub.sendSms = () => {

};

module.exports = pub;
